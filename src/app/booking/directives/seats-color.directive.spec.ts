import { SeatsColorDirective } from './seats-color.directive';

describe('SeatsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatsColorDirective();
    expect(directive).toBeTruthy();
  });
});
