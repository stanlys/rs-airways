import { SeatsBgcDirective } from './seats-bgc.directive';

describe('SeatsBgcDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatsBgcDirective();
    expect(directive).toBeTruthy();
  });
});
