import { FindFlightPipe } from './find-flight.pipe';

describe('FindFlightPipe', () => {
  it('create an instance', () => {
    const pipe = new FindFlightPipe();
    expect(pipe).toBeTruthy();
  });
});
